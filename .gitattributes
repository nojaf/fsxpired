# The parser tests record character offsets into the sample scripts, and the snapshots are
# compared byte for byte, so neither may change line endings on checkout.
* text=auto eol=lf
*.verified.txt text eol=lf working-tree-encoding=UTF-8
